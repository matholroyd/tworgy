%h1 Creating user

= error_messages_for :user

- form_for(:user, :url => objects_url) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag "Create"

= link_to 'Back', objects_path
